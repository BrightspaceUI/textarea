{
  "name": "d2l-textarea",
  "description": "Polymer-based web component for D2L text areas",
  "private": true,
  "scripts": {
    "postinstall": "polymer install --variants",
    "test:lint:html": "eslint *.html demo/*.html test/*.html",
    "test:lint:wc": "polymer lint",
    "test:lint": "npm run test:lint:html && npm run test:lint:wc",
    "test": "cross-env LAUNCHPAD_BROWSERS=chrome npm run test:lint && polymer test --skip-plugin sauce"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/BrightspaceUI/textarea.git"
  },
  "author": "D2L Corporation",
  "license": "Apache-2.0",
  "devDependencies": {
    "cross-env": "^5.1.4",
    "eslint": "^4.15.0",
    "eslint-config-brightspace": "^0.4.0",
    "eslint-plugin-html": "^4.0.1",
    "polymer-cli": "^1.5.7"
  }
}
